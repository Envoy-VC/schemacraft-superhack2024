{
  "name": "web3-turbo-starter",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier . --write --cache",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build --filter=eas-uikit  && changeset publish",
    "typedoc": "typedoc"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "^2.0.12",
    "typedoc": "^0.26.5",
    "typedoc-material-theme": "^1.1.0",
    "typedoc-plugin-extras": "^3.1.0"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@wagmi/cli": "^2.1.8"
  }
}
